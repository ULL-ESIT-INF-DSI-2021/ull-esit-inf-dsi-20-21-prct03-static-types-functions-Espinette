// Ejercicio 1 - Años bisiestos

/* function sum(year) {
  return year + year;
}

const bisiestos = sum(1900);
console.log(`bisiestos = ${bisiestos}`);
 */

function add(firstNum, secondNum) {
  return firstNum + secondNum;
}

const mySum = add(1, 7);
console.log(`mySum = ${mySum}`);
