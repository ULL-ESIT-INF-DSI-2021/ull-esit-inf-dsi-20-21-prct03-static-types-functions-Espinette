/* function ipsInRange(ip1: string, ip2: string) {
  // ip1 = ip1.replace(/[-]/g, "");
  let ip1num: number = 0;
  ip1num = parseInt(ip1, 10);
  let ip2num: number = 0;
  ip2num = parseInt(ip1, 10);
  if ()
  console.log(`Rango entre ${ip1num}`);
}

const ip1: string = "10.0.0.0";
const ip2: string = "10.0.0.50";
const solRangeIp = ipsInRange(ip1, ip2);
console.log(`Rango entre ${ip1} y ${ip2} : ${solRangeIp}`);
 */
